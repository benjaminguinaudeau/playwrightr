# bring_to_front <- function(page) page$bring_to_front()
# close <- function(page) page$close()
# reload <- function(page){ page$reload() ; return(invisible(page))}
# go_back <- function(page){ page$go_back() ; return(invisible(page))}
# go_forward <- function(page){ page$go_forward() ; return(invisible(page))}
# content <- function(page) page$content()
#
# goto <- function(page, url, timeout = 10000){ page$goto(url = url, timeout = timeout) ; return(invisible(page)) }
# title <- function(page) page$title()
# pdf <- function(page, path) page$pdf(path = path)
# screenshot <- function(page, path, full_page = F, type = "png", ...){
#   page$screenshot(path = path, full_page = full_page, type = type)
#   return(invisible(page))
# }
